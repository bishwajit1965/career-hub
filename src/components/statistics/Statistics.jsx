import SectionTitle from "../sectionTitle/SectionTitle";

const Statistics = () => {
  return (
    <div>
      <SectionTitle title="Statistics" description="" />
    </div>
  );
};

export default Statistics;
